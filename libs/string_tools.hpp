#ifndef SMART_SPLIT_H
#define SMART_SPLIT_H
#include <vector>
#include <string>

std::vector<std::string> smart_split(std::string s, int len);

#endif

#ifndef REPLACE_ALL_H
#define REPLACE_ALL_H
#include <string>

void replace_all(std::string* s, std::string to_replace, std::string by);

# endif
